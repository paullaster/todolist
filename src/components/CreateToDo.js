import Form from "./Form";
import LabeledInput from "./LabeledInput";

const CreateToDo = () => {
    <Form>
        <LabeledInput
        >
          Create to do
        </LabeledInput>
    </Form>
};

export default CreateToDo;